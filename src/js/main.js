window.addEventListener('DOMContentLoaded', () => {
    const finderButton = document.querySelector('#finder-button');

    finderButton.addEventListener('click', () => window.location.href = '../pages/appointment-finder.html');
});